<webscript>
	<shortname>Remove a reply from a document</shortname>
	<description><![CDATA[
		Removes the reply of a document. Also remove the attached elements
		(comments, attached-files, ...) and the reference to the main document.
		<p>
		Note that the format used here is different from the post definition. This
		is due to a problem/bug on Alfresco when trying to call this webscript in
		a JSON/DELETE fashion.
	]]>
	</description>
	<family>YaMma</family>
	<arguments>
	
	</arguments>
	<url>/bluexml/yamma/reply-mail/{store_type}/{store_id}/{id}</url>
	<format default="json" />
	<authentication runas="admin">user</authentication>
</webscript>
